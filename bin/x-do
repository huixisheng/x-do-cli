#!/usr/bin/env node

const commander = require('commander');

commander
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('view', '创建入口的view模板')
  .command('component', '创建组件')
  .parse(process.argv);